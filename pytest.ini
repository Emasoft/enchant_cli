[pytest]
addopts = -v --cov=enchant_cli --cov-report=term-missing
testpaths = tests
python_files = test_*.py
pythonpath = src
markers =
    api_key: mark test as requiring OPENROUTER_API_KEY environment variable
    timeout: set timeout for test # If using @pytest.mark.timeout
