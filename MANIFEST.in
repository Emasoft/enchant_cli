# Only include files not automatically handled by setuptools/pyproject.toml
include tests/samples/test_sample.txt
# recursive-include src... is usually not needed with pyproject.toml and find:packages
# global-exclude is also generally not needed for standard build artifacts
